CRUD תרגיל
1. NODE ולהכניס למשתנים בתוך (CLI = Command Line Interface / bash)CLI-לקבל פרמטרים מה
  1a. בדיקה: להדפיס את המשתנים ולראות שהערך נכון -- V --

2. ולהריץ אותן MAIN-לייבא אותן ל ,CREATE-ו READ לכתוב פונקציות
 2a. (console.log(`hi from read`) :למשל) בדיקה: לתת לפונקציות להדפיס סתם משהו -- V --


** לזכור תמיד להוסיף בדיקות שפיות!!

3. להריץ פונקציות מיובאות לפי סוג הפקודה
 3a. בדיקה: לעשות את זה